Review evidence for the mixed-vehicles + rear-end-crash PR.

Deliberately NOT under `public/` — Vite copies `public/` into `dist/` and the
GitHub Pages workflow publishes it, so review screenshots placed there would
ship to the live site. This directory is prunable once the PR is merged.
